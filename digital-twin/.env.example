# ===================
# API Metadata
# ===================
APP_NAME=Golden Retriever
APP_VERSION=0.1.0
APP_DESCRIPTION=AI-powered rail conflict resolution system using vector similarity search and digital twin simulation

# ===================
# Environment Settings
# ===================
# Options: dev, staging, prod
ENVIRONMENT=dev
HOST=0.0.0.0
PORT=8000

# CORS Settings (JSON array format)
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# ===================
# Qdrant Cloud Settings
# ===================
# Get these from your Qdrant Cloud dashboard: https://cloud.qdrant.io/
QDRANT_URL=https://your-cluster-id.region.cloud.qdrant.io:6333
QDRANT_API_KEY=your-qdrant-api-key-here
QDRANT_COLLECTION=rail_conflicts
QDRANT_TIMEOUT=30

# ===================
# Embedding Settings
# ===================
# Model from HuggingFace sentence-transformers
# Popular options: all-MiniLM-L6-v2 (384d), all-mpnet-base-v2 (768d)
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_DIMENSION=384
# Optional: cache directory for downloaded models
# EMBEDDING_CACHE_DIR=./models

# ===================
# Simulation Settings
# ===================
SIMULATION_TIMEOUT=30
MAX_RECOMMENDATIONS=5

# ===================
# Transitland API Settings
# ===================
# Get API key from: https://www.transit.land/
# Used for fetching real transit schedule data to generate realistic conflicts
TRANSITLAND_API_KEY=your-transitland-api-key-here
TRANSITLAND_CACHE_TTL=3600

# ===================
# Schedule-Based Conflict Generation
# ===================
# Ratio of schedule-based vs synthetic conflicts (0-1)
# 0.7 = 70% schedule-based, 30% synthetic
SCHEDULE_CONFLICT_RATIO=0.7
# Minimum minutes between trains on same platform
MIN_PLATFORM_TURNAROUND=3
# Minimum headway in seconds between consecutive trains
MIN_HEADWAY_SECONDS=180
