# FastAPI Application Dependencies (Python 3.13 compatible)
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.0
pydantic-settings==2.6.0

# Vector Database
qdrant-client==1.12.0

# AI/ML
sentence-transformers==3.3.1
numpy>=2.4.0  # Use numpy 2.x for Python 3.13 compatibility

# HTTP Client
httpx==0.27.0
requests==2.32.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.9

# CORS (built into FastAPI, no separate package needed)
# fastapi-cors is deprecated - using FastAPI's built-in CORS middleware
